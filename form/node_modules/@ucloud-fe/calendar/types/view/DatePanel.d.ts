import React from 'react';
import { DisabledFunc } from 'src/interface';
import { SharedPanelProps } from 'src/view/interface';
declare type DateBodyProps = SharedPanelProps & {
    disabledDate?: DisabledFunc;
};
declare const _default: React.MemoExoticComponent<({ value, onChange, rangeValue, current, onCurrentChange, now, disabledDate }: DateBodyProps) => JSX.Element>;
export default _default;
