import React, { ElementType } from 'react';
import { Key } from '../../hooks/group';
export interface CollapseProps {
    /** 激活的面板，multiple 时为数组值，controlled */
    openKeys?: Key[];
    /** 默认激活的面板，multiple 时为数组值，uncontrolled */
    defaultOpenKeys?: Key[];
    /** 变化回调 */
    onChange?: (keys: Key[]) => void;
    /** 是否可以多个同时打开 */
    multiple?: boolean;
    /**
     * @ignore
     * 自定义包裹容器组件
     */
    component?: ElementType;
}
declare const _default: React.MemoExoticComponent<({ openKeys, defaultOpenKeys, onChange, multiple, component, children, ...rest }: CollapseProps & Omit<React.HTMLAttributes<HTMLDivElement>, keyof CollapseProps>) => JSX.Element>;
export default _default;
