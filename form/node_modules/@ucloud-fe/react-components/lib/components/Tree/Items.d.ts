import React from 'react';
import { ChangeKeyMap, Group, LoadData, SelectedMap, TreeData } from './interface';
declare const MemoItems: React.MemoExoticComponent<({ children, depth, ignoreIndent, disabled, multiple, onSelect, selectedMap, group, loadData }: {
    children?: TreeData[] | undefined;
    depth: number;
    ignoreIndent?: number[] | undefined;
    disabled: boolean;
    multiple: boolean;
    onSelect: (keyMap: ChangeKeyMap) => void;
    selectedMap: SelectedMap;
    group: Group;
    loadData?: LoadData | undefined;
}) => JSX.Element | null>;
export default MemoItems;
