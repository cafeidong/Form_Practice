import { Key, ReactNode } from 'react';
export interface OptionProps {
    /** 值，会被用作 key */
    value?: Key;
    /** 是否禁用 */
    disabled?: boolean;
    /**
     * tooltip 提示，可以为文本或 node，也可以是 tooltip 的 props object
     */
    tooltip?: ReactNode | any;
    /** @ignore */
    children?: ReactNode;
    /**
     * @ignore
     * @description 内部使用，请勿使用
     */
    label?: ReactNode;
}
declare const Option: {
    ({ value, children, ...rest }: OptionProps): JSX.Element;
    isMenuItem: boolean;
};
export declare const PureOption: ({ value, label, children, ...rest }: OptionProps) => JSX.Element;
export default Option;
