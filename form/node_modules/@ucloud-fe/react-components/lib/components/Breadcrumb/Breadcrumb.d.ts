import { HTMLAttributes, ReactNode } from 'react';
import { StyleType } from './interface';
export interface BreadcrumbProps {
    /** 自定义分隔符 */
    separator?: ReactNode;
    /** 展示项激活样式的方式 */
    styleType?: StyleType;
}
declare const Breadcrumb: ({ separator, styleType, children, ...rest }: BreadcrumbProps & HTMLAttributes<HTMLElement>) => JSX.Element;
export default Breadcrumb;
