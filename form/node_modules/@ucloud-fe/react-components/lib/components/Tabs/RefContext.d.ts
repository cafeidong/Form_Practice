import React from 'react';
export declare type GetRef = (name: string) => HTMLElement | null;
export declare type SaveRef = (name: string) => (node: HTMLElement | null) => void;
declare const RefContext: React.Context<{
    getRef: GetRef;
    saveRef: SaveRef;
}>;
export default RefContext;
