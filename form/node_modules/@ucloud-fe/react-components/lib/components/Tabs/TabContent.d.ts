import React, { Key } from 'react';
import { Panes, TabBarPosition } from './shared';
interface TabContentProps {
    panes: Panes;
    activeKey: Key | null;
    tabBarPosition: TabBarPosition;
    destroyInactiveTabPane?: boolean;
    styleType: string;
    className?: string;
}
declare const _default: React.MemoExoticComponent<({ panes, activeKey, tabBarPosition, destroyInactiveTabPane, styleType, className }: TabContentProps) => JSX.Element>;
export default _default;
