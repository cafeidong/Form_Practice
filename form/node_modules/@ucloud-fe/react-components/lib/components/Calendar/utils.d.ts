import { DisabledFunc, TDate } from '@ucloud-fe/calendar';
import moment, { Moment } from 'moment';
export interface Rules {
    /** 范围值 */
    range?: [TDate, TDate];
    /** @deprecated */
    custom?: (date: Moment, value?: Moment | null) => boolean;
}
export declare const getDisabledRule: (rules?: Rules | undefined) => {
    date?: DisabledFunc;
    month?: DisabledFunc;
    year?: DisabledFunc;
    decade?: DisabledFunc;
};
export declare const isDateDisabled: (date: TDate, value?: TDate | null | undefined, rules?: Rules | undefined) => boolean | undefined;
export declare const getValidDate: (date: TDate, rules?: Rules | undefined) => moment.Moment;
