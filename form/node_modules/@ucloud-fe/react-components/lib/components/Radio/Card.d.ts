import React, { ReactNode } from 'react';
import { Override } from '../../type';
interface CardProps {
    title?: ReactNode;
    children?: ReactNode;
    checked?: boolean;
    disabled?: boolean;
    indeterminate?: boolean;
    multiple?: boolean;
}
declare const _default: React.MemoExoticComponent<({ title, children, checked, disabled, indeterminate, multiple, ...rest }: Override<React.HTMLAttributes<HTMLDivElement>, CardProps>) => JSX.Element>;
export default _default;
