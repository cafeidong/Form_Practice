import React, { ReactNode } from 'react';
import { Key } from '../../hooks/group';
export interface SubMenuProps {
    /** 标题内容 */
    title: ReactNode;
    /** 为弹出菜单、或是折叠层 */
    styleType?: 'collapse' | 'popover';
    /** 子菜单的唯一 key，也用作 collapse 的 panel 的 key，不存在时使用 key 代替 */
    subMenuKey?: Key;
    /** 禁用 */
    disabled?: boolean;
    /** @ignore */
    isFirst?: boolean;
    /** @ignore */
    isLast?: boolean;
}
declare const MemoSubMenu: ReturnType<typeof React.memo> & {
    isMenuSubMenu?: true;
};
export default MemoSubMenu;
