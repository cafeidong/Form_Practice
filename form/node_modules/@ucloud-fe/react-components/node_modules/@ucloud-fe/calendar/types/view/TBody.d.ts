import React, { ReactNode } from 'react';
import { CellValue, Mode } from 'src/interface';
interface TBodyProps<T extends Mode> {
    mode: T;
    cells: {
        className: string;
        children: ReactNode;
        disabled?: boolean;
        value?: CellValue<T>;
    }[];
    onCellClick: (cellIndex: number) => void;
    col: number;
    row: number;
}
declare function TBody({ cells, onCellClick, col, row, mode }: TBodyProps<Mode>): JSX.Element;
declare const _default: React.MemoExoticComponent<typeof TBody>;
export default _default;
